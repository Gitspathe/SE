using Microsoft.Xna.Framework.Graphics;

namespace SE.UI
{
    public class SlicedImage
    {
        public Texture2D Texture;

        public SlicedImage(Texture2D texture)
        {
            Texture = texture;
        }
    }
}
